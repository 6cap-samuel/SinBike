<resources>

    <!-- Base application theme. -->

    <!-- for text -->
    <style name="AppTabTextAppearance" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/black</item>   // default color
    </style>

    <style parent="Theme.AppCompat.Light.NoActionBar" name="Theme.PopMe">

        <item name="android:windowIsTranslucent">true</item>

        <item name="android:windowCloseOnTouchOutside">false</item>

    </style>
    <style name="Theme"/>

</resources>
